# Training hyperparameters
batch_size: 32
epochs: 5
seed: 42

# Optimizer configuration
optimizer:
    name: Adam  # Options: Adam, SGD, AdamW, RMSprop, etc.
    lr: 0.001
    weight_decay: 0

print_every: 100