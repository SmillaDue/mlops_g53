# Training hyperparameters
batch_size: 32
epochs: 10
seed: 42

# Optimizer configuration
optimizer:
    name: AdamW  # Options: Adam, SGD, AdamW, RMSprop, etc.
    lr: 0.0001
    weight_decay: 0.001

print_every: 50